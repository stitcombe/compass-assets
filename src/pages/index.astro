---
import MainLayout from '../layouts/main.astro';
import { categories } from '../lib/data';
import { Button } from '../components/ui/button';
---

<MainLayout title="Compass Assets" description="Browse and download our brand assets">
  <section class="container py-10">
    <div class="mx-auto max-w-[800px] space-y-6 text-center">
      <h1 class="text-4xl font-bold tracking-tight sm:text-5xl">Brand Asset Library</h1>
      <p class="text-muted-foreground">
        Browse, preview, and download our brand assets for your projects. All assets are organized by category for easy access.
      </p>
    </div>
  </section>

  <section class="container py-8">
    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
      {categories.map((category) => (
        <a href={category.path} class="group relative overflow-hidden rounded-lg border bg-card p-6 text-card-foreground shadow transition-all hover:shadow-md">
          <div class="flex flex-col items-center text-center">
            <div class="mb-4 rounded-full bg-primary/10 p-3 text-primary">
              {category.icon === "ImageIcon" && (
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-6 w-6">
                  <rect width="18" height="18" x="3" y="3" rx="2" ry="2"></rect>
                  <circle cx="9" cy="9" r="2"></circle>
                  <path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"></path>
                </svg>
              )}
              {category.icon === "FileIcon" && (
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-6 w-6">
                  <path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"></path>
                  <polyline points="14 2 14 8 20 8"></polyline>
                </svg>
              )}
              {category.icon === "PaletteIcon" && (
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-6 w-6">
                  <circle cx="13.5" cy="6.5" r=".5"></circle>
                  <circle cx="17.5" cy="10.5" r=".5"></circle>
                  <circle cx="8.5" cy="7.5" r=".5"></circle>
                  <circle cx="6.5" cy="12.5" r=".5"></circle>
                  <path d="M12 2C6.5 2 2 6.5 2 12s4.5 10 10 10c.926 0 1.648-.746 1.648-1.688 0-.437-.18-.835-.437-1.125-.29-.289-.438-.652-.438-1.125a1.64 1.64 0 0 1 1.668-1.668h1.996c3.051 0 5.555-2.503 5.555-5.554C21.965 6.012 17.461 2 12 2z"></path>
                </svg>
              )}
              {category.icon === "TypeIcon" && (
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="h-6 w-6">
                  <polyline points="4 7 4 4 20 4 20 7"></polyline>
                  <line x1="9" x2="15" y1="20" y2="20"></line>
                  <line x1="12" x2="12" y1="4" y2="20"></line>
                </svg>
              )}
            </div>
            <h3 class="text-xl font-semibold">{category.name}</h3>
            <p class="mt-2 text-sm text-muted-foreground">{category.description}</p>
            <div class="mt-4">
              <Button variant="outline" size="sm" class="group-hover:bg-primary group-hover:text-primary-foreground">
                Browse {category.name}
              </Button>
            </div>
          </div>
        </a>
      ))}
    </div>
  </section>

  <section class="container py-10">
    <div class="mx-auto max-w-[800px] space-y-4 rounded-lg border bg-card p-6 text-card-foreground shadow">
      <h2 class="text-2xl font-semibold">How to Use the Asset Library</h2>
      <div class="space-y-4 text-muted-foreground">
        <p>
          This asset library provides easy access to all brand assets in one place. Here's how to use it:
        </p>
        <ol class="ml-6 list-decimal space-y-2">
          <li>Browse assets by category using the cards above or the navigation menu.</li>
          <li>Use the search and filter options to find specific assets.</li>
          <li>Preview assets before downloading to ensure they meet your needs.</li>
          <li>Download assets with a single click or copy URLs to use in your projects.</li>
          <li>For colors, copy the HEX, RGB, or HSL values directly to your clipboard.</li>
        </ol>
        <p class="pt-2">
          If you have any questions or need additional assets, please contact the brand team.
        </p>
      </div>
    </div>
  </section>
</MainLayout>
